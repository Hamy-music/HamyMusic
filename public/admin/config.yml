backend:
  name: git-gateway
  branch: main 
  identity_url: https://hamymusic.netlify.app/.netlify/identity 
  
media_folder: static/kuvat
public_folder: /kuvat
collections:

  - name: 'fi-about'
    label: 'about (FI)'
    folder: 'content/fi'
    create: true
    slug: '{{year}}-{{month}}-{{day}}-{{slug}}'
    editor:
      preview: true
      preview_path: "/{{slug}}"  
    fields:
      - { label: 'Title', name: 'title', widget: 'string' }
      - { label: 'Intro Video URL', name: 'introvideo_src', widget: 'string' }
      - { label: 'Body', name: 'body', widget: 'markdown' }

  - name: 'fi-gigs'
    label: 'Performances (FI)'
    folder: 'content/fi'
    create: true
    slug: '{{year}}-{{month}}-{{day}}-{{slug}}'
    editor:
      preview: true
      preview_path: "/{{slug}}"  
    fields:
      - { label: 'Gig Image', name: 'gig_image', widget: 'image' }
      - { label: 'Gig Description', name: 'gig_description', widget: 'markdown' }

  - name: 'fi-diskografia'
    label: 'Discography (FI)'
    folder: 'content/fi'
    create: true
    slug: '{{year}}-{{month}}-{{day}}-{{slug}}'
    editor:
      preview: true
      preview_path: "/{{slug}}"  
    fields:
      - { label: 'Album/Singlen Kansikuva', name: 'album_image', widget: 'image' }
      - { label: 'Nimi', name: 'album_title', widget: 'string' }
      - { label: 'Vuosi', name: 'album_year', widget: 'string' }
      - { label: 'Tyypi (albumi/single)', name: 'album_type', widget: 'select', options: ['albumi', 'single'] }

  - name: 'fi-musicvideos'
    label: 'Music Videos (FI)'
    folder: 'content/fi'
    create: true
    slug: '{{year}}-{{month}}-{{day}}-{{slug}}'
    editor:
      preview: true
      preview_path: "/{{slug}}"  
    fields:
      - { label: 'Video ID', name: 'video_id', widget: 'string' }

  - name: 'fi-media'
    label: 'Media (FI)'
    folder: 'content/fi'
    create: true
    slug: '{{year}}-{{month}}-{{day}}-{{slug}}'
    editor:
      preview: true
      preview_path: "/{{slug}}"  
    fields:
      - { label: 'Media Image', name: 'media_image', widget: 'image' }
      - { label: 'Caption', name: 'caption', widget: 'string' }

  - name: 'fi-contact'
    label: 'Contact (FI)'
    folder: 'content/fi'
    create: true
    slug: '{{year}}-{{month}}-{{day}}-{{slug}}'
    editor:
      preview: true
      preview_path: "/{{slug}}"  
    fields:
      - { label: 'Contact Information', name: 'contact_info', widget: 'markdown' }


  - name: 'en-about'
    label: 'about (EN)'
    folder: 'content/en'
    create: true
    slug: '{{year}}-{{month}}-{{day}}-{{slug}}'
    editor:
      preview: true
      preview_path: "/{{slug}}"  
    fields:
      - { label: 'Title', name: 'title', widget: 'string' }
      - { label: 'Intro Video URL', name: 'introvideo_src', widget: 'string' }
      - { label: 'Body', name: 'body', widget: 'markdown' }

  - name: 'en-gigs'
    label: 'Performances (EN)'
    folder: 'content/en'
    create: true
    slug: '{{year}}-{{month}}-{{day}}-{{slug}}'
    editor:
      preview: true
      preview_path: "/{{slug}}"  
    fields:
      - { label: 'Gig Image', name: 'gig_image', widget: 'image' }
      - { label: 'Gig Description', name: 'gig_description', widget: 'markdown' }


  - name: 'en-diskografia'
    label: 'Discography (EN)'
    folder: 'content/en'
    create: true
    slug: '{{year}}-{{month}}-{{day}}-{{slug}}'
    editor:
      preview: true
      preview_path: "/{{slug}}"  
    fields:
      - { label: 'Album/Singlen Kansikuva', name: 'album_image', widget: 'image' }
      - { label: 'Nimi', name: 'album_title', widget: 'string' }
      - { label: 'Vuosi', name: 'album_year', widget: 'string' }
      - { label: 'Tyypi (albumi/single)', name: 'album_type', widget: 'select', options: ['album', 'single'] }


  - name: 'en-musicvideos'
    label: 'Music Videos (EN)'
    folder: 'content/en'
    create: true
    slug: '{{year}}-{{month}}-{{day}}-{{slug}}'
    editor:
      preview: true
      preview_path: "/{{slug}}"  
    fields:
      - { label: 'Video ID', name: 'video_id', widget: 'string' }


  - name: 'en-media'
    label: 'Media (EN)'
    folder: 'content/en'
    create: true
    slug: '{{year}}-{{month}}-{{day}}-{{slug}}'
    editor:
      preview: true
      preview_path: "/{{slug}}"  
    fields:
      - { label: 'Media Image', name: 'media_image', widget: 'image' }
      - { label: 'Caption', name: 'caption', widget: 'string' }

  - name: 'en-contact'
    label: 'Contact (EN)'
    folder: 'content/en'
    create: true
    slug: '{{year}}-{{month}}-{{day}}-{{slug}}'
    editor:
      preview: true
      preview_path: "/{{slug}}"  
    fields:
      - { label: 'Contact Information', name: 'contact_info', widget: 'markdown' }     
